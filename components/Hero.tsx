"use client"; import { useState, useRef } from "react"; import styles from "./Hero.module.css"; export default function Hero() { const [isPlaying, setIsPlaying] = useState(true); const [isMuted, setIsMuted] = useState(true); const videoRef = useRef<HTMLVideoElement>(null); const togglePlay = () => { if (videoRef.current) { if (isPlaying) { videoRef.current.pause(); } else { videoRef.current.play(); } setIsPlaying(!isPlaying); } }; const toggleMute = () => { if (videoRef.current) { videoRef.current.muted = !isMuted; setIsMuted(!isMuted); } }; return ( <section id="home" className={styles.heroContainer}> <video ref={videoRef} className={styles.bgVideo} autoPlay loop muted={isMuted} playsInline poster="/projects/farming-assistant.png"> <source src="/hero-video.mp4" type="video/mp4" /> </video> <div className={styles.overlay}></div> <div className={styles.content}> <div className={styles.leftCol}> <span className={styles.portfolioTag}>PORTFOLIO 2026</span> <h1 className={styles.mainTitle}>Nithish</h1> <p className={styles.tagsLine}>AI-POWERED FULL-STACK DEVELOPER • DATA ANALYTICS • SYSTEM DESIGN</p> <div className={styles.btnRow}> <a href="#projects" className={styles.viewProjectsBtn}>View Projects ↗</a> </div> </div> </div> <aside className={styles.socialBar}> <a href="https://instagram.com" target="_blank" rel="noreferrer">INSTAGRAM</a> <a href="https://github.com/rnithish18" target="_blank" rel="noreferrer">GITHUB</a> <a href="https://linkedin.com" target="_blank" rel="noreferrer">LINKEDIN</a> </aside> <div className={styles.videoControls}> <button onClick={togglePlay} aria-label="Toggle Play"> {isPlaying ? "⏸" : "▶"} </button> <button onClick={toggleMute} aria-label="Toggle Mute"> {isMuted ? "🔇" : "🔊"} </button> </div> </section> ); }