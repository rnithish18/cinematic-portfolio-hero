"use client"; import { CERTIFICATIONS } from "@/lib/data"; import styles from "./CertificationsSection.module.css"; export default function CertificationsSection() { return ( <section id="certifications" className={styles.certSection}> <div className={styles.header}> <span className={styles.eyebrow}>VERIFIED CREDENTIALS</span> <h2 className={styles.title}>Certifications & Achievements</h2> </div> <div className={styles.grid}> {CERTIFICATIONS.map((cert) => ( <div key={cert.title} className={styles.card}> <div className={styles.cardTop}> <span className={styles.issuer}>{cert.issuer}</span> <span className={styles.date}>{cert.date}</span> </div> <h3 className={styles.certTitle}>{cert.title}</h3> <div className={styles.skillsRow}> {cert.skills.map((skill) => ( <span key={skill} className={styles.skillTag}>{skill}</span> ))} </div> {cert.credentialUrl && ( <a href={cert.credentialUrl} target="_blank" rel="noreferrer" className={styles.verifyLink}>Verify Credential ↗</a> )} </div> ))} </div> </section> ); }